# Spec 04: GallerySection

## File
`app/components/GallerySection.vue`

## Behavior
- Wrapper section for the gallery.
- Uses `UPageSection` with `id="gallery"` so the nav anchor scrolls to it.

## Layout
- Contains `GalleryAccordion` as its sole content.

## Notes
- No additional content beyond the accordion (no heading, no copy).
- See spec 05 for `GalleryAccordion` details.
